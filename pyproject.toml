[project]
name = "qr2-megakernel"
version = "0.1.0"
description = "Recursive Householder QR with fused panel megakernels"
requires-python = ">=3.12"
dependencies = [
    "cuda-bindings>=13.0",
    "numpy",
    "torch>=2.12.0",
]

[tool.uv]
package = false
prerelease = "allow"
