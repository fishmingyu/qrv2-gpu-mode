[project]
name = "qr2-megakernel"
version = "0.1.0"
description = "Recursive Householder mega-kernels for batched FP32 QR"
requires-python = ">=3.12"
dependencies = [
    "cuda-bindings>=13.0",
    "torch>=2.12.0",
]

[tool.uv]
package = false
prerelease = "allow"
